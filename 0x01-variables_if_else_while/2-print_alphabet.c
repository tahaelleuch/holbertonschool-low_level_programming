#include <stdio.h>
/**
 * main - main block
 * Description : using the function "putchar" to print all the aplhbaets
 * Return: 0
 */
int main (void)
{
	char ch = 'a';

	while (c <= 'z')
	{
		putchar (ch);
		ch = ch + 1;
	}
	putchar ("\n");

	return (0);
}
