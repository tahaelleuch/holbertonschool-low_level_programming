#include "holberton.h"

/**
 * add: adds two integers and returns the resukt.
 * @a: integer to add with @b
 * @b: integer to add with @a
 * Return: the addition of @a + @b
*/
int add(int a, int b)
{

	return(a + b);
}
